/target
**/*.rs.bk
Cargo.lock.orig

# Generated fixtures and fitted artifacts. Corpora are never committed:
# only fitted profiles with a provenance manifest are shareable, and even
# those need a privacy review when derived from private data.
/fixtures/demo/
*.jsonl
!crates/**/tests/**/*.jsonl

# Python eval harness
__pycache__/
*.egg-info/
.venv/

/corpus/
